<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TodoApp.Views.MakeTodoPage"
             Title="Create Todo"
             x:Name="MakeTodoPageViewModel">
    <ContentPage.Content>
        <StackLayout Margin="20" VerticalOptions="StartAndExpand" >
            <Label Text="Name" Margin="5" FontSize="Medium" />
            <Entry Text="{Binding Name}" />
            <Label Text="Notes" Margin="5" FontSize="Medium" />
            <Entry Text="{Binding Notes}" />
            <Label Text="Done" Margin="5" FontSize="Medium" />
            <Switch IsToggled="{Binding Done}" HorizontalOptions="Center"/>
            <Button Text="Save" Clicked="OnSaveClicked" />
            <Button Text="Delete" Clicked="OnDeleteClicked" />
            <Button Text="Cancel" Clicked="OnCancelClicked"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>